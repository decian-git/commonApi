account:
      type: object
      properties:
        name:
          type: string
        accountType:
          type: string
        address:
          type: string
        info:
          $ref: 'https://raw.githubusercontent.com/decian-git/commonApi/master/info.yaml'
      required:
      - name
      - accountType

info:
      type: object
      properties:
        sentAt:
          type: string
          format: date-time